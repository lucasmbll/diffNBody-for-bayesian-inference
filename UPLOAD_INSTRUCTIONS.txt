# GitHub instruction

# 1. Stage all changes in the current folder (nbody-inverse-sampling)
git add .

# 2. Commit your changes with a message
git commit -m "Update: Circular velocity enclosed mass, video generation, jupyter notebook report, energy tracking, density scaling"

# 3. Push to your remote repository (e.g., origin, main branch)
git push origin main

# WARNING: This will overwrite remote changes on main!
git push --force origin main

# Use tmux 

tmux new -s sim1blob
tmux new -s sim2blobs
tmux new -s sim3
s
leave : Ctrl + b (hold Ctrl, press b, release both), then press d

re attach : tmux attach -t mysession OR IF ONLY ONE SESSION tmux a

list all sessions : tmux ls

kill a session : tmux kill-session -t mysession


python src/run_experiments.py --config configs/default_sim.yaml

Current sessions name :
sampling
 tmux attach -t sampling_nuts
sampling_nuts
